\documentclass[10pt, twocolumn]{article}
\usepackage[margin=0.75in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{booktabs}
\usepackage{float}
\usepackage{caption}

\title{\textbf{TensorSwarm: Biologically-Inspired Secure Federated Learning for Edge IoT Devices}}

\author{
  Cavin Krenik \\
  Olympic College \\
  Shelton, WA, USA \\
  \texttt{cavinkrenik@icloud.com}
}

\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, but traditional approaches struggle with the constraints of edge IoT environments: limited bandwidth, heterogeneous hardware, and vulnerability to attacks. We present \textbf{TensorSwarm} (formerly QRES), an edge-native FL system that redefines compression as shared prediction, achieving up to 22:1 ratios on time-series sensor data while incorporating robust privacy and security mechanisms. Inspired by biological neural pathways and swarm intelligence, TensorSwarm uses spiking neural networks (SNNs) for temporal prediction, differential privacy (DP) for inference resistance, secure aggregation via pairwise masking to prevent honest-but-curious peers, and zero-knowledge proofs (ZK) for update validity. Implemented in Rust with \texttt{no\_std} compatibility for bare-metal deployment, our system ensures bit-perfect determinism using Q16.16 fixed-point arithmetic, enabling reproducible ``federated dreaming'' across swarms. Evaluations on Azure-simulated clusters (up to 100 nodes) show 20-30\% overhead for full security stack, with regime change recovery in 3-4 rounds. Compared to baselines like FedAvg and FedProx, TensorSwarm offers superior Byzantine tolerance (up to 49\% malicious nodes via Krum) at comparable convergence speeds. This work bridges information theory and distributed AI, providing a privacy-preserving framework for bandwidth-constrained IoT applications.
\end{abstract}

\vspace{0.2cm}
\noindent \textbf{Keywords:} Federated Learning, Edge AI, Spiking Neural Networks, Secure Aggregation, Differential Privacy, Zero-Knowledge Proofs, IoT Compression

\section{Introduction}
The proliferation of Internet of Things (IoT) devices has generated massive data streams at the edge, yet utilizing this data for machine learning remains challenging due to bandwidth constraints and privacy concerns. Traditional centralized training requires transmitting raw data to the cloud, incurring high latency, bandwidth costs, and security risks. Federated Learning (FL) \cite{mcmahan2017communication} addresses this by training models locally and aggregating updates, but existing frameworks like FedAvg or FedProx \cite{li2020federated} are often too heavyweight for constrained edge devices and lack intrinsic support for asynchronous, biologically-inspired learning dynamics suited for temporal sensor data.

Furthermore, deploying FL in adversarial environments necessitates robust security. Privacy attacks can reconstruct training data from gradients \cite{geyer2017differentially}, while Byzantine adversaries can poison the global model \cite{blanchard2017machine}. Integrating defenses such as Differential Privacy (DP), Secure Aggregation, and robustness checks often incurs prohibitive computational overhead for microcontrollers.

To address these gaps, we propose \textbf{TensorSwarm}, a holistic FL system designed for the edge. By treating compression as a prediction problem, TensorSwarm minimizes bandwidth usage while learning temporal representations. Our key contributions are:
\begin{enumerate}
    \item \textbf{Bio-mimetic Edge Learning:} A lightweight SNN-based predictor ensemble that learns temporal patterns with high energy efficiency \cite{maass1997networks}, implemented in `no\_std` Rust.
    \item \textbf{Bit-Perfect Determinism:} A novel Q16.16 fixed-point arithmetic engine ensuring identical model behavior across heterogeneous hardware (x86, ARM, RISC-V).
    \item \textbf{Comprehensive Security Stack:} A layered defense integrating $\epsilon$-Differential Privacy \cite{dwork2014algorithmic}, Pairwise-Masking Secure Aggregation \cite{bonawitz2017practical}, and Zero-Knowledge Proofs for update validity.
    \item \textbf{Reproducible Evaluation:} Extensive benchmarks on 100-node Azure clusters demonstrating scalability, resilience, and superior compression ratios compared to standard codecs.
\end{enumerate}

\section{Background}
\subsection{Spiking Neural Networks (SNNs)}
Unlike Artificial Neural Networks (ANNs), SNNs operate on discrete spikes, mirroring biological neurons. This allows for extreme energy efficiency and effective modeling of temporal data \cite{roy2019towards}. TensorSwarm leverages SNNs with Generalized Integrate-and-Fire (GIF) neurons to predict sensor time-series data, enabling higher compression by transmitting only prediction residuals.

\subsection{Secure Federated Learning}
Security in FL is multi-faceted. \textbf{Differential Privacy (DP)} adds noise to gradients to mask individual contributions \cite{abadi2016deep}, preventing membership inference. \textbf{Secure Aggregation} ensures the aggregator sees only the sum of updates, not individual inputs \cite{bonawitz2017practical}. \textbf{Byzantine Resilience} algorithms like Krum \cite{blanchard2017machine} filter out malicious outliers. TensorSwarm combines all three into a configurable stack.

\subsection{Q16.16 Fixed-Point Arithmetic}
Floating-point non-determinism across architectures poses a major reproducibility crisis in distributed systems. TensorSwarm enforces strict determinism using Q16.16 fixed-point math, guaranteeing that a model trained on a Raspberry Pi behaves identically to one on a server, a critical requirement for consensus in decentralized swarms.

\section{System Design}
\subsection{Architecture Overview}
TensorSwarm follows a decoupled architecture (Figure \ref{fig:arch}). The \textbf{Core} handles deterministic compression and prediction, while the \textbf{Daemon} manages P2P networking and swarm intelligence.
\begin{figure}[h]
\centering
\includegraphics[width=0.48\textwidth]{figures/figure1_architecture.pdf} % User must provide this
\caption{TensorSwarm system architecture showing the decoupled Core (predictor ensemble) and Daemon (swarm coordinator) with the layered security stack.}
\label{fig:arch}
\end{figure}

\subsection{Components}
\begin{itemize}
    \item \textbf{qres\_core:} A `no\_std` crate containing the predictor ensemble (Linear, Spectral, SNN). It runs on bare metal or WASM.
    \item \textbf{qres\_daemon:} The swarm coordinator built on `libp2p`. It manages the \textbf{MetaBrain}, a Reinforcement Learning (PPO) agent that adaptively re-weights predictors based on local data distributions.
\end{itemize}

\subsection{Security Stack}
\begin{enumerate}
    \item \textbf{Layer 1: Differential Privacy.} Local updates $ \Delta w $ are clipped and noised: $\Delta w' = \text{clip}(\Delta w, C) + \mathcal{N}(0, \sigma^2)$.
    \item \textbf{Layer 2: Secure Aggregation.} Using Diffie-Hellman (X25519) key exchange, pairs of nodes generate shared masks that cancel out upon summation, protecting individual values.
    \item \textbf{Layer 3: Zero-Knowledge Proofs.} Nodes attach Pedersen commitments and proofs that their update norm is within bounds, preventing poisoning without revealing the update.
\end{enumerate}

\subsection{Regime Change Adaptation}
Similar to neural plasticity, TensorSwarm adapts to data drifts ("regime changes"). The MetaBrain detects increased prediction error (surprise) and triggers rapid re-weighting or recruiting of new "epiphanies" (model weights) from the swarm, ensuring long-term resilience.

\section{Implementation}
Implemented in \textbf{Rust}, TensorSwarm prioritizes safety and performance.
\begin{itemize}
    \item \textbf{Efficiency:} Rust's zero-cost abstractions allow high-level logic with C++ comparable speed.
    \item \textbf{Portability:} The `qres\_core` compiles to WASM for browser deployment and standard binaries for Linux/Windows/macOS.
    \item \textbf{Reproducibility:} Docker containers and an Azure-based simulation harness facilitate reproducible benchmarks.
\end{itemize}

\noindent \textbf{Fixed-Point Snippet:}
\begin{verbatim}
// Q16.16 fixed-point example
pub struct Fixed16 { raw: i32 }
impl Fixed16 {
    pub fn mul(self, other: Fixed16) -> Fixed16 {
        let p = (self.raw as i64) * 
                (other.raw as i64);
        Fixed16 { raw: (p >> 16) as i32 }
    }
}
\end{verbatim}

\section{Evaluation}
We evaluated TensorSwarm on various datasets (IoT anomaly, synthetic drift) using an Azure cluster of 10-100 Standard B1s VMs.

\subsection{Privacy Overhead}
Table \ref{tab:privacy} and Figure \ref{fig:privacy} show the cost of security. While full security incurs ~30ms latency (20-30\% overhead) per round, this is acceptable for the guarantees provided.
\begin{table}[h]
\centering
\caption{Privacy Overhead Analysis}
\label{tab:privacy}
\begin{tabular}{lcccc}
\toprule
Mode & Loss (\%) & Time (ms) & Energy & Mem (KB) \\
\midrule
Baseline & 0 & 0 & 100k & 200 \\
DP Only & 10 & 5 & 110k & 210 \\
Secure Agg & 5 & 20 & 120k & 300 \\
Full Stack & 20 & 30 & 130k & 350 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.45\textwidth]{figures/figure2_privacy_overhead.pdf}
\caption{Runtime overhead increases with security levels but remains within practical limits for edge devices.}
\label{fig:privacy}
\end{figure}

\subsection{Regime Change Resilience}
Figure \ref{fig:regime} demonstrates TensorSwarm's ability to recover from concept drift. The system recovers accuracy within 3-5 rounds after abrupt or gradual shifts.
\begin{figure}[h]
\centering
\includegraphics[width=0.45\textwidth]{figures/figure3_regime_change.pdf}
\caption{Accuracy recovery following different types of regime changes at round 10.}
\label{fig:regime}
\end{figure}

\subsection{Baseline Comparison}
Comparing against FedAvg and FedProx (Figure \ref{fig:baseline}), TensorSwarm achieves comparable convergence speeds but uniquely offers Byzantine resilience (up to 49\% malicious nodes).
\begin{figure}[h]
\centering
\includegraphics[width=0.45\textwidth]{figures/figure4_baseline_comparison.pdf}
\caption{Convergence comparison. TensorSwarm aligns closely with FedProx while adding robust security.}
\label{fig:baseline}
\end{figure}

\subsection{Scalability}
Figure \ref{fig:scale} validates performance on up to 100 nodes. Protocol overhead scales linearly, maintaining >85\% success rate.
\begin{figure}[h]
\centering
\includegraphics[width=0.45\textwidth]{figures/figure5_scalability.pdf}
\caption{Scalability of sync and aggregation times vs. number of nodes.}
\label{fig:scale}
\end{figure}

\section{Discussion}
TensorSwarm is best suited for privacy-critical IoT networks size <50 nodes. The computational overhead of masking ($O(n^2)$ naive, optimized to $O(n \log n)$ here) trade-offs well against the security benefits. Future work includes investigating threshold homomorphic encryption.

\section{Related Work}
Extensive research exists in optimization (FedAvg \cite{mcmahan2017communication}, FedProx \cite{li2020federated}) and security (Secure Aggregation \cite{bonawitz2017practical}). However, few systems combine these with bio-inspired SNNs \cite{maass1997networks} and strict determinism for the edge. TensorSwarm fills this gap by integrating these diverse fields into a cohesive \texttt{no\_std} Rust framework.

\section{Conclusion}
TensorSwarm presents a novel, biologically-inspired approach to secure Federated Learning. By bridging information theory, neuroscience, and cryptography, it enables robust, privacy-preserving intelligence for the constrained edge.

\bibliographystyle{plain}
\bibliography{references}

\end{document}
